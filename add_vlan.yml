---
- name: Show Running Config
  hosts: switches
  connection: network_cli
  gather_facts: no

  tasks:
    - name: Add vlan
      ios_vlans:
        config:
          - name: Vlan_220
            vlan_id: 220
            state: active
        state: merged
